public class Fly implements Comparable<Fly> {

    @Override
    public String toString() {return null;}

    // Sorterer flyene fra foerste til siste avgangstid
    @Override
    public int compareTo(Fly annen) {return 0;}
}